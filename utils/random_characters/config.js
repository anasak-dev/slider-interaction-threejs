/**
 * Config for swapping letters
 * @param {Boolean} stayRunning changes to true if loop is running and vice versa
 * @param {Number} timeout setInterval timeout
 */

export const config = {
  elementClass: ".chars",
  elementWrapperClass: ".encryptedCard",
  timeout: 200,
};
